<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <!-- Room Form -->
        <record model="ir.ui.view" id="room_form">
            <field name="name">room.room.form</field>
            <field name="model">room.room</field>
            <field name="arch" type="xml">
                <form string="Room">
                    <sheet>
                        <div class="oe_button_box" name="button_box">
                            <button name="toggle_active" type="object"
                                class="oe_stat_button" icon="fa-archive">
                                <field name="active" widget="boolean_button"
                                    options="{&quot;terminology&quot;: &quot;archive&quot;}" />
                            </button>
                        </div>
                        <group>
                            <group>
                                <field name="name" required="1" />
                                <field name="clinic_id" options="{'no_create_edit': True}" />
                            </group>
                        </group>
                    </sheet>
                </form>
            </field>
        </record>

        <!-- Room List -->
        <record model="ir.ui.view" id="room_room_tree">
            <field name="name">room.room.tree</field>
            <field name="model">room.room</field>
            <field name="arch" type="xml">
                <tree string="Room">
                    <field name="name" />
                    <field name="clinic_id" invisible="1"/>
                    <field name="active" widget="boolean_toggle" />
                </tree>
            </field>
        </record>

        <!-- Room Search -->
        <record model="ir.ui.view" id="room_room_search">
            <field name="name">room.room.search</field>
            <field name="model">room.room</field>
            <field name="arch" type="xml">
                <search>
                    <field name="name" />
                    <filter string="Archived" name="inactive"
                        domain="[('active','=',False)]" />
                </search>
            </field>
        </record>

        <!-- Room Action -->
        <record model="ir.actions.act_window" id="room_room_action">
            <field name="name">Rooms</field>
            <field name="res_model">room.room</field>
            <field name="view_mode">tree,form</field>
        </record>

        <!-- Room Menu -->
        <menuitem id="room_menu" action="room_room_action"
            parent="bista_tdcc_operations.miscellaneous_configuration_menu" />

</odoo>
